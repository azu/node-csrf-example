<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CSRF Example</title>
    <style>
        .main {
            max-width: 720px;
            margin: auto;
            text-align: center;
        }
    </style>
</head>
<body>
<main class="main">
    {{#if isLogin }}
        <p>Hello {{userName}}!</p>
        <p>Thanks to register!</p>

        <h1>Unregister form</h1>
        <form method="POST" action="{{baseURL}}/unregister">
            <label>
                <input type="hidden" name="_csrf" value="{{csrfToken}}"/>
                <input type="submit" value="Unregister"/>
            </label>
        </form>
    {{else}}
        <p>You can register this service!</p>
        <h1>Register form</h1>
        <form method="POST" action="{{baseURL}}/register">
            <label>UserName:
                <input type="text" name="username"/>
                <input type="hidden" name="_csrf" value="{{csrfToken}}"/>
                <input type="submit" value="Register"/>
            </label>
        </form>
    {{/if}}
</main>
</body>
</html>
